import 'package:flutter/material.dart';class ThankYouPage extends StatelessWidget {  final String stateName;  final String countryName;  const ThankYouPage({Key key, this.stateName, this.countryName}) : super(key: key);  @override  Widget build(BuildContext context) {    return Scaffold(      appBar: AppBar(        title: Text('Thank you!'        ),      ),      body: Center(        child: Text('Congrats Your selected data \nState: $stateName \n Country $countryName',style: TextStyle(          fontSize: 20,        ),),      ),    );  }}